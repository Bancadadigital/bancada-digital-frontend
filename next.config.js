/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["via.placeholder.com"], // Adicione qualquer outro domínio necessário aqui
  },
};

module.exports = nextConfig;
